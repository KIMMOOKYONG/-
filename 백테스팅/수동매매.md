```python
dollars = 5_000_000
balance = 0
hld_shares = 0
tot_values = 0

from pykrx import stock
import pandas as pd
import numpy as np
from datetime impor datetime

def download(ticker="005930", fromdate="20200101", todate="20220905"):
    df = stock.get_market_ohlcv_by_date(fromdate=fromdate, todate=todate, ticker=ticker)
    df = df.reset_index()
    df.columns = ["Date", "Open", "High", "Low", "Close", "Volume"]

    print("데이터 다운로드 완료")
    for t in range(len(df)):
        yield df.iloc[t]


for a in download():
    print(a["Date"], a["Open"], a["High"], a["Low"], a["Close"], a["Volume"])
    action, amount = input("매수[1],매도[2],홀딩[0]을 선택하세요?").split()

    if action == "1":
        cash = cash - (a["Close"] * int(amount))
        hld_shares = hld_shares + int(amount)
        balance = a["Close"] * hld_shares
        tot_values = cash + balance
        print("현금: ", cash, " 잔고: ", balance, " 주식: ", hld_shares, " 총액: ", tot_values)
    elif action == "2":
        cash = cash + (a["Close"] * int(amount))
        hld_shares = hld_shares - int(amount)
        balance = a["Close"] * hld_shares
        tot_values = cash + balance
        print("현금: ", cash, " 잔고: ", balance, " 주식: ", hld_shares, " 총액: ", tot_values)
    elif action == "0":
        balance = a["Close"] * hld_shares
        tot_values = cash + balance
        print("현금: ", cash, " 잔고: ", balance, " 주식: ", hld_shares, " 총액: ", tot_values)  
    else:
        balance = balance + (a["Close"] * hld_shares)
        tot_values = cash + balance
        print("현금: ", cash, " 잔고: ", balance, " 주식: ", hld_shares, " 총액: ", tot_values)          
        break

```
